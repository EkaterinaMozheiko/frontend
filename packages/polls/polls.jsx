const React = require('react');
const { div } = require('react-dom');

const Poll = () => {
    return (
        <div className="poll-application">
            "NEW PAGE"
        </div>
    )
};

export default Poll;